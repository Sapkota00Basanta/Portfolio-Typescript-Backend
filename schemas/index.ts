// Import User-Defined Modules
import portfolioSchemaList from './portfolioSchemaList'

/**
 * Import all the schemas which are to be used for
 * managing content of our webisite.
 */
export const schemaTypes = [
  portfolioSchemaList.aboutSchema,
  portfolioSchemaList.brandSchema,
  portfolioSchemaList.contactSchema,
  portfolioSchemaList.experienceSchema,
  portfolioSchemaList.skillSchema,
  portfolioSchemaList.testimonialSchema,
  portfolioSchemaList.workExperienceSchema,
  portfolioSchemaList.workSchema,
]
